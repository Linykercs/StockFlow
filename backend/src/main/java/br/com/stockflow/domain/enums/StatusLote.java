package br.com.stockflow.domain.enums;

/** Status calculado de um lote, usado para exibir os badges do design system (ok/baixo/vencendo/vencido). */
public enum StatusLote {
    EM_ESTOQUE,
    BAIXO,
    VENCENDO,
    VENCIDO
}
